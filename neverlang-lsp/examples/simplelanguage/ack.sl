function sum(m,n) {
    if (m == 0) {
        return m + n;
    } else {
        return false;
        // return m + n;
    }
}

function sum2(m,n) {
    if (m == 0) {
        return m + n;
    } else {
        //return false;
        return m + n;
    }
}

function tmp() {
    return 0;
}

function tmp1() {
    return 1;
}

function main() {
    test = "ciao";
    test2 = false;

    s = 0;
    tmp = tmp();
    tmp1 = tmp1();

    x = sum(0, 1);
    return 0;
}
